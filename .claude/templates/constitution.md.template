---
paths:
---

# 工作空间宪法

以下规则具有最高优先级，任何情况下不得违反。

## 禁止手动编辑的区域

以下文件或区域由工具自动生成/管理，禁止手动编辑：

<!-- TODO: 根据项目实际情况添加条目，例如：
- `**/generated/` — 代码生成产物
- `go.sum` — 依赖锁定文件（由 go mod 管理）
- `package-lock.json` — 依赖锁定文件（由 npm 管理）
- `dist/` — 构建输出目录
-->

## 工作流程

1. 提交代码前必须确认构建和测试通过
2. 修改代码前必须查阅对应项目的 `.claude/rules/`，包括其 `constitution.md`
3. 仅在需要阅读或修改某个子工程时，才加载其 `CLAUDE.md` 和 rules，不要一次性全部加载

## 代码质量

- 所有错误必须显式处理，禁止静默忽略（如 Go 的 `_ = err`、JS 的空 `catch`）
- 日志输出必须包含足够的上下文信息（时间、模块、关键参数），便于线上排查
- 新增功能必须附带对应的单元测试或集成测试
- 删除代码时同步删除相关测试，不留死代码

## 安全规则

- 密钥、Token、密码等敏感信息禁止硬编码，必须通过环境变量或密钥管理服务获取
- `.env`、`credentials.json` 等敏感文件必须加入 `.gitignore`，禁止提交到仓库
- 第三方依赖引入前需确认其安全性和维护状态，避免引入已知漏洞的版本
- 数据库查询必须使用参数化查询，禁止拼接 SQL

## 子工程索引

| 工程 | CLAUDE.md | Rules 路径 | 说明 |
|------|-----------|-----------|------|
<!-- TODO: 添加子工程条目，格式示例：
| `server` | [`CLAUDE.md`](../../server/CLAUDE.md) | `server/.claude/rules/` | 游戏服务端 |
| `client` | [`CLAUDE.md`](../../client/CLAUDE.md) | `client/.claude/rules/` | 游戏客户端 |
-->
