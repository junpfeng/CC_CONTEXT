# 项目简介

<!-- TODO: 填写项目名称和简要描述，例如：
MyProject —— 一个基于微服务架构的在线游戏平台，包含游戏服务端、AI 系统和管理后台。
-->

# 技术栈

<!-- TODO: 列出项目使用的主要技术栈，例如：
- 语言：Go 1.21 / Rust / TypeScript
- 框架：Gin / Actix / Next.js
- 数据库：PostgreSQL / Redis
- 基础设施：Docker / Kubernetes
- CI/CD：GitHub Actions
-->

# 项目目录结构

| 目录 | 说明 | CLAUDE.md |
|------|------|-----------|
<!-- TODO: 添加子工程条目，格式示例：
| `server/` | 游戏服务端 | [`CLAUDE.md`](server/CLAUDE.md) |
| `client/` | 游戏客户端 | [`CLAUDE.md`](client/CLAUDE.md) |
| `shared/` | 公共库 | [`CLAUDE.md`](shared/CLAUDE.md) |
-->

> **按需加载规则**：仅在需要阅读或修改某个子工程时，才读取其 `CLAUDE.md`。不要一次性加载所有子工程的说明。

# 工作规范

- **最小上下文原则：优先用 subagent 完成任务，主 agent 只做调度和总结，不堆积中间结果**
- 先阅读代码再改，不要猜测未检查的代码
- 代码注释用中文，变量命名用英文
- 不确定的地方询问我，不要瞎猜

# 快速命令

<!-- TODO: 填写项目常用命令，例如：
```bash
# 构建
make build

# 测试
make test

# 运行
make run

# 代码检查
make lint
```
-->

# .claude 目录

| 路径 | 说明 |
|------|------|
| `rules/constitution.md` | 工作空间宪法（最高优先级规则） |
| `skills/` | 工作流技能（dev-workflow、dev-debug 等） |
| `templates/` | 通用文档模板套件 |
| `INDEX.md` | 完整索引 |

> 各子工程有各自的 `.claude/rules/` 宪法文件。
