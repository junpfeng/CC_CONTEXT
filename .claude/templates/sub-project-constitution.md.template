---
paths:
  - <!-- TODO: 填写本工程的路径匹配模式，例如：server/**/* -->
---

# 子工程宪法

<!-- TODO: 填写子工程名称 -->

以下规则在本工程范围内具有最高优先级。本宪法是工作空间宪法的补充，不得与其冲突。

> 工作空间宪法 → [`constitution.md`](../../../.claude/rules/constitution.md)

## 禁止操作

<!-- TODO: 列出本工程特有的禁止操作，例如：
- 禁止直接修改 `generated/` 目录下的文件（由 protoc 生成）
- 禁止绕过 ORM 直接写裸 SQL（特殊场景需在 PR 中说明原因）
- 禁止在 `internal/` 包中导出供外部使用的类型
-->

## 代码风格

<!-- TODO: 列出本工程的代码风格强制规则，例如：
- 遵循 Go 官方代码风格，使用 `gofmt` 格式化
- 函数命名使用 camelCase，导出函数使用 PascalCase
- 错误类型统一使用 `fmt.Errorf("模块名: %w", err)` 格式包装
- 常量使用 UPPER_SNAKE_CASE
-->

## 架构约束

<!-- TODO: 列出本工程的架构强制约束，例如：
- 依赖方向：handler → service → repo，禁止反向依赖
- `internal/` 包之间禁止循环依赖
- 外部服务调用必须通过接口抽象，便于测试 mock
- 配置项必须集中管理，禁止在代码中硬编码配置值
-->
