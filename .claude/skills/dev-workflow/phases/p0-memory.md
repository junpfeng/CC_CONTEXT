# Phase 0：记忆查询

> 领域依赖：无

在正式开始前，查询历史经验。

## 步骤

1. **提取关键词**：从需求描述中提取 2-3 个核心关键词
2. **查阅项目记忆**：
   - **Auto Memory**（`MEMORY.md`）中的相关章节
   - **项目文档**（参照项目 `CLAUDE.md` 或 `docs/` 目录下的文档）
3. **输出摘要**：命中则列出可复用思路和需要避免的坑；未命中则输出"未找到相关历史记忆"

## 容错

Phase 0 不阻塞主流程，搜索失败不影响后续阶段。
